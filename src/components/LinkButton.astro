---
import { Icon } from 'astro-icon/components';

const { label, href, reverse, invert, icon = true, ...attrs } = Astro.props;

let order = -1;

if (reverse) {
    order = 1;
}
---

<a class:list={['link-button', { 'link-button--invert': invert }]} href={href} {...attrs}>
    {icon && <Icon name="logo" size="30" />}
    <span><slot>{label}</slot></span>
</a>

<style lang="scss" define:vars={{ order }}>
    .link-button {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        text-decoration: none;

        svg {
            transition: transform 0.4s ease-out;
            order: var(--order);
        }

        span {
            font-family: var(--urbanistFont);
            font-weight: 400;
            text-transform: uppercase;

            display: inline-block;
            position: relative;
            border-bottom: 1px solid var(--backgroundDarkColor);
            padding: 0.1rem 0.1rem;

            &::after {
                content: '';
                position: absolute;
                width: 100%;
                transform: scaleX(0);

                height: 2px;
                bottom: 0;
                left: 0;
                background-color: var(--accentColor);
                transform-origin: bottom right;
                transition: transform 0.25s ease-out;
            }

            &:hover::after {
                transform: scaleX(1);
                transform-origin: bottom left;
            }
        }
        &:hover {
            span::after {
                transform: scaleX(1);
                transform-origin: bottom left;
            }

            svg {
                transform: rotateZ(360deg);
            }
        }
    }

    .link-button--invert {
        color: var(--backgroundColor);

        span {
            border-bottom-color: var(--foregroundDarkColor);

            &::after {
                background-color: var(--accentColor);
            }
        }

        svg {
            color: var(--backgroundColor);
        }
    }

    @keyframes rotateSVG {
        from {
            transform: rotate(0deg);
        }
        to {
            transform: rotate(360deg);
        }
    }
</style>
